INICIO
    ESCREVA ("QUALQUER COISA 1234 5 6 666").
    var := LEIA().
    var2 := 12.
    literal := "Variavel do tipo literal". ++

    SE var > var2 ENTÃO
        ESCREVA("MAIOR").
    SENÃO
        ESCREVA("menor").

    PARA INTEIRO i:=0 ATÉ 10 PASSO 2
        ESCREVA(i*i).
    FIMPARA.
FIM.